version: '3.8'

services:
  bitcask:
    build: .
    container_name: bitcask-server
    ports:
      - "50051:50051"
    volumes:
      - bitcask_data:/data/bitcask
    restart: unless-stopped

volumes:
  bitcask_data:
